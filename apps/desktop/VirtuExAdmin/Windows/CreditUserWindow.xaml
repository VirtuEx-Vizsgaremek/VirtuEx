<ui:FluentWindow x:Class="VirtuExAdmin.Windows.CreditUserWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="Add funds"
                 Width="420"
                 Height="260"
                 MinWidth="420"
                 MinHeight="260"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 WindowCornerPreference="Default"
                 WindowBackdropType="Mica"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" Text="Add funds" />
            <TextBlock x:Name="SubtitleText" Margin="0,4,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="" />
        </StackPanel>

        <TextBlock Grid.Row="2" Text="AMOUNT" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
        <ui:TextBox Grid.Row="3" x:Name="AmountTextBox" PlaceholderText="e.g. 1000" PreviewTextInput="AmountTextBox_PreviewTextInput" />

        <!-- Currency: keep simple for now (default USD), easy to extend later -->
        <TextBlock Grid.Row="4" Margin="0,10,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Currency: USD" />

        <WrapPanel Grid.Row="5" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button x:Name="CancelButton" Content="Cancel" Appearance="Secondary" Width="110" Margin="0,0,8,0" Click="Cancel_Click" />
            <ui:Button x:Name="SubmitButton" Content="Submit" Appearance="Primary" Width="110" Click="Submit_Click" />
        </WrapPanel>
    </Grid>
</ui:FluentWindow>
